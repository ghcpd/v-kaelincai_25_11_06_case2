# Project A - Pre-Feature (Broken UI)

This project intentionally produces overlapping value labels for charts.

Usage:
- Install dependencies: `bash setup.sh`
- Run tests: `bash run_tests.sh` or `pytest -q tests`

Output:
- `results/results_pre.json`: test results and metrics
- `results/*.png`: saved chart images for visual inspection
