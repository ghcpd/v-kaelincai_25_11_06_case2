# Project B - Post-Feature (Fixed UI)

Improvements implemented:
- Dynamic label placement to avoid overlaps
- Text stroke for improved contrast

Usage:
- Install dependencies: `bash setup.sh`
- Run tests: `bash run_tests.sh` or `pytest -q tests`

Output:
- `results/results_post.json`: test results and metrics
- `results/*.png`: saved chart images for visual inspection
