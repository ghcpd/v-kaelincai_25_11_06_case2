# Shared Artifacts

- `test_data.json`: canonical set of test cases used for both projects.
- `compare_report.md`: aggregated comparison report showing before/after screenshots and metrics.
- `run_all.sh`: top-level script to run both projects and create the comparison.
