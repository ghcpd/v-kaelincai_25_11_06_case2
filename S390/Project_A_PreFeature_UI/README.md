# Project A - Pre-Feature (Buggy UI)

This project contains a basic chart generator that places value labels directly above bars without collision avoidance.

How to run:
- On Windows PowerShell: .\run_tests.ps1
- On Unix: ./run_tests.sh

Results will be written to `results/results_pre.json` with generated sample charts saved in `results/`.
