# Project B - Post-Feature (Fixed UI)

This project contains an improved chart generator that dynamically avoids label overlap and optimizes label contrast.

How to run:
- On Windows PowerShell: .\run_tests.ps1
- On Unix: ./run_tests.sh

Results will be written to `results/results_post.json` with generated charts saved in `results/`.
